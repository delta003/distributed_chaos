# This script should start node on given port
# Script takes one parameter, port number
# Node must be started in background and redirect all output to /dev/null
# Node should assume that bootstrap is running on port 9080
