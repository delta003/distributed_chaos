# Pokretanje test client-a
# Koriscenje ./test

CWD="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

if g++ -O2 -std=c++11 $CWD/test.cpp -I/usr/local/include/boost -o $CWD/bin/test -lboost_system; then
  $CWD/bin/test
  rm $CWD/bin/test
fi
